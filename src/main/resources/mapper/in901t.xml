<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.insrb.app.mapper.IN901TMapper">

	<insert id="insert">
		insert into in901t(quote_no, tmpl_id, tmpl_name, mobile, result_code, result_msg, cmid, message)
		values (#{quote_no}, #{tmpl_id}, #{tmpl_name}, #{mobile}, #{result_code}, #{result_msg}, #{cmid}, #{message})
	</insert> 

</mapper>